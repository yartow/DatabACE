}
# concatenate column value
temp_row <- paste0(
temp_row,
if_else(isFirstColumn, "", ", "), # add a comma except in case of the first column
def_value
)
# Check if we are in the first column
if(isFirstColumn){
# we are in the first column, but now we will go to the second one
isFirstColumn <- FALSE
}
}
# Add parenthesis and line feed
temp_row <- paste0(temp_row, ")\n")
temp_row
# append the temporary row to the temporary query
temp_query <- paste0(
temp_query,
temp_row
)
temp_query
# create SQL file
temp_row <- ""
temp_query <- ""
isFirstColumn <- TRUE
for(m in 1:nrow(temp_table)){
temp_row <- paste0("insert into ", tableNames[i], " values (")
# Concatenate all column values
for (n in 1:ncol(temp_table)){
temp_value <- temp_table[[n]][m]
if(is.na(temp_value)){
def_value <- "NULL"
} else {
if (typeof(temp_value) == CHAR){
def_value <- paste0("'", temp_value, "'")
} else {
def_value <- temp_value
}
}
# concatenate column value
temp_row <- paste0(
temp_row,
if_else(isFirstColumn, "", ", "), # add a comma except in case of the first column
def_value
)
# Check if we are in the first column
if(isFirstColumn){
# we are in the first column, but now we will go to the second one
isFirstColumn <- FALSE
}
}
# END THE ROW ####
# Add parenthesis and line feed
temp_row <- paste0(temp_row, ")\n")
# append the temporary row to the temporary query
temp_query <- paste0(
temp_query,
temp_row
)
# # reset the temporary row
# temp_row <- ""
isFirstColumn <- TRUE
}
# write the file. Note that writing this will overwrite the previous file
write(temp_query, sql_populate_file_location)
dbFileLocation
getwd()
codeDirectory
source(paste0(codeDirectory, "/db_info.R"))
CODE_DIRECTORY <- paste0(HOME_DIRECTORY, CODE_FOLDER)
rm(codeDirectory)
sql_populate_file_location
currentTableName <- tableNames[i]
# get table name
source(paste0(CODE_DIRECTORY, "db_info.R"))
# get table name
source(paste0(CODE_DIRECTORY, "db_info.R"))
# Create file names
sql_populate_fileName <- paste0("populate", currentTableName, EXT_SQL)
sql_populate_file_location <- paste0(codeDirectory, sql_populate_fileName)
i <- 1
sql_populate_file_location <- paste0(CODE_DIRECTORY, sql_populate_fileName)
sql_populate_file_location
currentTableName <- tableNames[i]
# Create file names
sql_populate_fileName <- paste0("populate", currentTableName, EXT_SQL)
sql_populate_file_location <- paste0(CODE_DIRECTORY, sql_populate_fileName)
source(paste0(getwd(), "/createSQL.R"))
source(paste0(CODE_DIRECTORY, "/createSQL.R"))
source(paste0(CODE_DIRECTORY, "createSQL.R"))
CODE_DIRECTORY <- paste0(HOME_DIRECTORY, CODE_FOLDER, "/RDatabACE")
currentTableName <- tableNames[i]
# read file
temp_table <- read.xlsx(
dbFileLocation,
sheetName = currentTableName
) %>% as_tibble()
# Create file names
sql_populate_fileName <- paste0("populate", currentTableName, EXT_SQL)
sql_populate_file_location <- paste0(CODE_DIRECTORY, sql_populate_fileName)
CODE_DIRECTORY <- paste0(HOME_DIRECTORY, CODE_FOLDER, "/RDatabACE/")
sql_populate_file_location <- paste0(CODE_DIRECTORY, sql_populate_fileName)
# get table name
source(paste0(CODE_DIRECTORY, "db_info.R"))
sql_populate_file_location <- paste0(CODE_DIRECTORY, sql_populate_fileName)
source(paste0(CODE_DIRECTORY, "createSQL.R"))
sql_populate_file_location
CODE_DIRECTORY_SQL <- paste0(HOME_DIRECTORY, CODE_FOLDER)
CODE_DIRECTOR_R <- paste0(HOME_DIRECTORY, CODE_FOLDER, "RDatabACE/")
CODE_DIRECTORY_R <- paste0(HOME_DIRECTORY, CODE_FOLDER, "RDatabACE/")
rm(CODE_DIRECTOR_R, CODE_DIRECTORY)
# get table name
source(paste0(CODE_DIRECTORY_R, "db_info.R"))
# Create file names
sql_populate_fileName <- paste0("populate", currentTableName, EXT_SQL)
sql_populate_file_location <- paste0(CODE_DIRECTORY_SQL, sql_populate_fileName)
# Create file names
header(temp_table)
# Create file names
names(temp_table)
temp_query <- paste0("-- Columns", names(temp_table))
# testing purposes
i <- 3
currentTableName <- tableNames[i]
# read file
temp_table <- read.xlsx(
dbFileLocation,
sheetName = currentTableName
) %>% as_tibble()
# Create file names
sql_populate_fileName <- paste0("populate", currentTableName, EXT_SQL)
sql_populate_file_location <- paste0(CODE_DIRECTORY_SQL, sql_populate_fileName)
source(paste0(CODE_DIRECTORY, "createSQL.R"))
source(paste0(CODE_DIRECTORY_R, "createSQL.R"))
# testing purposes
i <- 4
currentTableName <- tableNames[i]
# read file
temp_table <- read.xlsx(
dbFileLocation,
sheetName = currentTableName
) %>% as_tibble()
sql_populate_file_location <- paste0(CODE_DIRECTORY_SQL, sql_populate_fileName)
source(paste0(CODE_DIRECTORY_R, "createSQL.R"))
currentTableName
# get table name
source(paste0(CODE_DIRECTORY_R, "db_info.R"))
# testing purposes
i <- 5
currentTableName <- tableNames[i]
# read file
temp_table <- read.xlsx(
dbFileLocation,
sheetName = currentTableName
) %>% as_tibble()
# Create file names
sql_populate_fileName <- paste0("populate", currentTableName, EXT_SQL)
sql_populate_file_location <- paste0(CODE_DIRECTORY_SQL, sql_populate_fileName)
source(paste0(CODE_DIRECTORY_R, "createSQL.R"))
gsub("'", "''", "Arent van 's-Gravesandestraat 29")
source(paste0(CODE_DIRECTORY_R, "createSQL.R"))
typeof(temp_table)
temp_table
temp_table[[7]][1]
typeof(temp_table[[7]][1])
typeof(temp_table[[7]][1])
class(temp_table[[7]][1])
DATE
DATE <- "Date"
def_value <- paste0("TO_DATE('", temp_value, "', 'YYYY-MM-DD'")
def_value
# create SQL file
temp_row <- ""
temp_query <- "" # paste0("-- Columns", names(temp_table))
isFirstColumn <- TRUE
for(m in 1:nrow(temp_table)){
temp_row <- paste0("insert into ", currentTableName, " values (")
# Concatenate all column values
for (n in 1:ncol(temp_table)){
temp_value <- temp_table[[n]][m]
if(is.na(temp_value)){
# the value is null
def_value <- "NULL"
} else {
# the value is not null
if (typeof(temp_value) == CHAR){
# the value is of the type string
# replace apostrophes
temp_value <- gsub("'", "''", temp_value)
def_value <- paste0("'", temp_value, "'")
} else {
# the type of this value is not a string
# check if the type is a date
def_value <- if_else(class(temp_value) == DATE,
# the type is a date. Add SQL format to date
paste0("TO_DATE('", temp_value, "', 'YYYY-MM-DD')"),
temp_value
)
}
}
# concatenate column value
temp_row <- paste0(
temp_row,
if_else(isFirstColumn, "", ", "), # add a comma except in case of the first column
def_value
)
# Check if we are in the first column
if(isFirstColumn){
# we are in the first column, but now we will go to the second one
isFirstColumn <- FALSE
}
}
# END THE ROW ####
# Add parenthesis and line feed
temp_row <- paste0(temp_row, ")\n")
# append the temporary row to the temporary query
temp_query <- paste0(
temp_query,
temp_row
)
# # reset the temporary row
# temp_row <- ""
isFirstColumn <- TRUE
}
rlang:last_error()
rlang::last_error()
rlang::last_trace()
paste0("TO_DATE('", temp_value, "', 'YYYY-MM-DD')")
temp_value
for(m in 1:nrow(temp_table)){
temp_row <- paste0("insert into ", currentTableName, " values (")
# Concatenate all column values
for (n in 1:ncol(temp_table)){
temp_value <- temp_table[[n]][m]
if(is.na(temp_value)){
# the value is null
def_value <- "NULL"
} else {
# the value is not null
if (typeof(temp_value) == CHAR){
# the value is of the type string
# replace apostrophes
temp_value <- gsub("'", "''", temp_value)
def_value <- paste0("'", temp_value, "'")
} else {
# the type of this value is not a string
# check if the type is a date
def_value <- ifelse(class(temp_value) == DATE,
# the type is a date. Add SQL format to date
paste0("TO_DATE('", temp_value, "', 'YYYY-MM-DD')"),
temp_value
)
}
}
# concatenate column value
temp_row <- paste0(
temp_row,
if_else(isFirstColumn, "", ", "), # add a comma except in case of the first column
def_value
)
# Check if we are in the first column
if(isFirstColumn){
# we are in the first column, but now we will go to the second one
isFirstColumn <- FALSE
}
}
# END THE ROW ####
# Add parenthesis and line feed
temp_row <- paste0(temp_row, ")\n")
# append the temporary row to the temporary query
temp_query <- paste0(
temp_query,
temp_row
)
# # reset the temporary row
# temp_row <- ""
isFirstColumn <- TRUE
}
# write the file. Note that writing this will overwrite the previous file
write(temp_query, sql_populate_file_location)
if(is.na(temp_value)){
# the value is null
def_value <- "NULL"
} else {
# the value is not null
if (typeof(temp_value) == CHAR){
# the value is of the type string
# replace apostrophes
temp_value <- gsub("'", "''", temp_value)
def_value <- paste0("'", temp_value, "'")
} else {
# the type of this value is not a string
# check if the type is a date
def_value <- ifelse(class(temp_value) == DATE,
# the type is a date. Add SQL format to date
paste0("cast('", temp_value, "' as Date)"),
temp_value
)
}
}
# concatenate column value
temp_row <- paste0(
temp_row,
if_else(isFirstColumn, "", ", "), # add a comma except in case of the first column
def_value
)
# Check if we are in the first column
if(isFirstColumn){
# we are in the first column, but now we will go to the second one
isFirstColumn <- FALSE
}
# Concatenate all column values
for (n in 1:ncol(temp_table)){
temp_value <- temp_table[[n]][m]
if(is.na(temp_value)){
# the value is null
def_value <- "NULL"
} else {
# the value is not null
if (typeof(temp_value) == CHAR){
# the value is of the type string
# replace apostrophes
temp_value <- gsub("'", "''", temp_value)
def_value <- paste0("'", temp_value, "'")
} else {
# the type of this value is not a string
# check if the type is a date
def_value <- ifelse(class(temp_value) == DATE,
# the type is a date. Add SQL format to date
paste0("cast('", temp_value, "' as Date)"),
temp_value
)
}
}
# concatenate column value
temp_row <- paste0(
temp_row,
if_else(isFirstColumn, "", ", "), # add a comma except in case of the first column
def_value
)
# Check if we are in the first column
if(isFirstColumn){
# we are in the first column, but now we will go to the second one
isFirstColumn <- FALSE
}
}
# Add parenthesis and line feed
temp_row <- paste0(temp_row, ")\n")
# append the temporary row to the temporary query
temp_query <- paste0(
temp_query,
temp_row
)
# # reset the temporary row
# temp_row <- ""
isFirstColumn <- TRUE
for(m in 1:nrow(temp_table)){
temp_row <- paste0("insert into ", currentTableName, " values (")
# Concatenate all column values
for (n in 1:ncol(temp_table)){
temp_value <- temp_table[[n]][m]
if(is.na(temp_value)){
# the value is null
def_value <- "NULL"
} else {
# the value is not null
if (typeof(temp_value) == CHAR){
# the value is of the type string
# replace apostrophes
temp_value <- gsub("'", "''", temp_value)
def_value <- paste0("'", temp_value, "'")
} else {
# the type of this value is not a string
# check if the type is a date
def_value <- ifelse(class(temp_value) == DATE,
# the type is a date. Add SQL format to date
paste0("cast('", temp_value, "' as Date)"),
temp_value
)
}
}
# concatenate column value
temp_row <- paste0(
temp_row,
if_else(isFirstColumn, "", ", "), # add a comma except in case of the first column
def_value
)
# Check if we are in the first column
if(isFirstColumn){
# we are in the first column, but now we will go to the second one
isFirstColumn <- FALSE
}
}
# END THE ROW ####
# Add parenthesis and line feed
temp_row <- paste0(temp_row, ")\n")
# append the temporary row to the temporary query
temp_query <- paste0(
temp_query,
temp_row
)
# # reset the temporary row
# temp_row <- ""
isFirstColumn <- TRUE
}
# write the file. Note that writing this will overwrite the previous file
write(temp_query, sql_populate_file_location)
# for each worksheet in the Excel file, convert the text to SQL queries
for (i in 1:length(tableNames)){
# testing purposes
# i <- 5
currentTableName <- tableNames[i]
# read file
temp_table <- read.xlsx(
dbFileLocation,
sheetName = currentTableName
) %>% as_tibble()
# Create file names
sql_populate_fileName <- paste0("populate", currentTableName, EXT_SQL)
sql_populate_file_location <- paste0(CODE_DIRECTORY_SQL, sql_populate_fileName)
source(paste0(CODE_DIRECTORY_R, "createSQL.R"))
}
print(paste0("Creating ", sql_populate_fileName))
# get table name
source(paste0(CODE_DIRECTORY_R, "db_info.R"))
# for each worksheet in the Excel file, convert the text to SQL queries
for (i in 1:length(tableNames)){
print(paste0("Creating ", sql_populate_fileName))
# testing purposes
# i <- 5
currentTableName <- tableNames[i]
# read file
temp_table <- read.xlsx(
dbFileLocation,
sheetName = currentTableName
) %>% as_tibble()
# Create file names
sql_populate_fileName <- paste0("populate", currentTableName, EXT_SQL)
sql_populate_file_location <- paste0(CODE_DIRECTORY_SQL, sql_populate_fileName)
source(paste0(CODE_DIRECTORY_R, "createSQL.R"))
}
tableNames
# read file
temp_table <- read.xlsx(
dbFileLocation,
sheetName = currentTableName
) %>% as_tibble()
# Create file names
sql_populate_fileName <- paste0("populate", currentTableName, EXT_SQL)
sql_populate_file_location <- paste0(CODE_DIRECTORY_SQL, sql_populate_fileName)
source(paste0(CODE_DIRECTORY_R, "createSQL.R"))
# for each worksheet in the Excel file, convert the text to SQL queries
for (i in 1:length(tableNames)){
print(paste0("Creating ", sql_populate_fileName))
# testing purposes
# i <- 5
currentTableName <- tableNames[i]
# read file
temp_table <- read.xlsx(
dbFileLocation,
sheetName = currentTableName
) %>% as_tibble()
# Create file names
sql_populate_fileName <- paste0("populate", currentTableName, EXT_SQL)
sql_populate_file_location <- paste0(CODE_DIRECTORY_SQL, sql_populate_fileName)
source(paste0(CODE_DIRECTORY_R, "createSQL.R"))
}
# for each worksheet in the Excel file, convert the text to SQL queries
for (i in 1:length(tableNames)){
print(paste0("Creating ", sql_populate_fileName))
# testing purposes
# i <- 5
currentTableName <- tableNames[i]
# read file
temp_table <- read.xlsx(
dbFileLocation,
sheetName = currentTableName
) %>% as_tibble()
# Create file names
sql_populate_fileName <- paste0("populate", currentTableName, EXT_SQL)
sql_populate_file_location <- paste0(CODE_DIRECTORY_SQL, sql_populate_fileName)
source(paste0(CODE_DIRECTORY_R, "createSQL.R"))
}
currentTableName
# get table name
source(paste0(CODE_DIRECTORY_R, "db_info.R"))
currentTableName <- tableNames[i]
currentTableName
# read file
temp_table <- read.xlsx(
dbFileLocation,
sheetName = currentTableName
) %>% as_tibble()
# for each worksheet in the Excel file, convert the text to SQL queries
for (i in 1:length(tableNames)){
print(paste0("Creating ", sql_populate_fileName))
# testing purposes
# i <- 5
currentTableName <- tableNames[i]
# read file
temp_table <- read.xlsx(
dbFileLocation,
sheetName = currentTableName
) %>% as_tibble()
# Create file names
sql_populate_fileName <- paste0("populate", currentTableName, EXT_SQL)
sql_populate_file_location <- paste0(CODE_DIRECTORY_SQL, sql_populate_fileName)
source(paste0(CODE_DIRECTORY_R, "createSQL.R"))
}
grepl("*", currentTableName)
grepl("*", currentTableName, fixed = TRUE)
